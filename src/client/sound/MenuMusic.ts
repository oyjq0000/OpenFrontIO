import type { AudioMixer } from "./AudioMixer";

/**
 * The upstream menu theme is proprietary. The game-library fork deliberately
 * leaves menu music disabled until an original or otherwise licensed track is supplied.
 */
export function startMenuMusic(_mixer: AudioMixer): void {
  // Intentionally empty.
}
